import * as React from "react";
export const ReceivedProps = ({ count }) => {
  return (
    <div>
      <div>REACT 19 host ReceiveProps with default export</div>
      <span>Count: {count}</span>
    </div>
  );
};

export default ReceivedProps;
